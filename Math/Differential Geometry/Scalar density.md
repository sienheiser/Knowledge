Using the [[Volume forms|volume form]] we can define a scalar density in a chart $(U,x)$ as 
$$
\omega_{(x)} := \Omega_{i_1,...,i_d}\epsilon^{i_1,...,i_d}
$$

One can show that 
$$
\omega_{(y)} = \text{det}(\frac{\partial x}{\partial y})\omega_{(x)}
$$